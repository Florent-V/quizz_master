<nav class="bg-base-100 shadow-md">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <!-- Logo et Titre -->
            <div class="flex-shrink-0">
                <a href="/" class="flex items-center">
                    <img class="h-8 w-auto" src="https://placehold.co/600x400" alt="Logo">
                    <span class="ml-3 text-xl font-bold text-base-content">SaaSApp</span>
                </a>
            </div>

            <!-- Liens de Navigation (Desktop) -->
            <div class="hidden lg:flex items-center space-x-4">
                <a href="/" class="text-base-content hover:text-primary px-3 py-2 rounded-md font-medium">Accueil</a>
                <a href="/product" class="text-base-content hover:text-primary px-3 py-2 rounded-md font-medium">Fonctionnalités</a>
                <a href="#" class="text-base-content hover:text-primary px-3 py-2 rounded-md font-medium">Tarifs</a>
                <a href="#" class="text-base-content hover:text-primary px-3 py-2 rounded-md font-medium">Contact</a>
            </div>

            <!-- Boutons & Hamburger -->
            <div class="flex items-center">
                <!-- Theme Toggle (Desktop) -->
                <div class="hidden lg:flex">
                    <div {{ vue_component('ThemeToggle') }}></div>
                </div>

                <!-- Boutons de connexion (Desktop) -->
                <div class="hidden lg:flex items-center ml-4">
                    {% if app.user %}
                        <div class="dropdown dropdown-end">
                            <label tabindex="0" class="btn btn-ghost btn-circle avatar">
                                <div class="w-10 rounded-full">
                                    <img src="{{ app.user.picture ? vich_uploader_asset(app.user, 'pictureFile') : asset('build/images/default_profil_picture.png') }}" alt="Avatar">
                                </div>
                            </label>
                            <ul tabindex="0" class="mt-3 z-[1] p-2 shadow menu menu-sm dropdown-content bg-base-100 rounded-box w-52">
                                <li><a href="/" class="text-base-content hover:text-primary font-medium">Accueil</a></li>
                                <li><a href="/product" class="text-base-content hover:text-primary font-medium">Fonctionnalités</a></li>
                                <li><a href="#" class="text-base-content hover:text-primary font-medium">Tarifs</a></li>
                                <li><a href="#" class="text-base-content hover:text-primary font-medium">Contact</a></li>
                                <div class="divider my-1"></div>
                                <!-- Theme Toggle (Mobile) -->
                                <li class="lg:hidden">
                                    <div class="flex justify-between items-center">
                                        <span>Thème</span>
                                        <div {{ vue_component('ThemeToggle') }}></div>
                                    </div>
                                </li>
                                <div class="divider my-1"></div>
                                {% if app.user %}
                                    <li><a href="{{ path('app_user_account') }}" class="text-base-content hover:text-primary font-medium">Votre Profil</a></li>
                                    <li><a class="text-base-content hover:text-primary font-medium">Paramètres</a></li>
                                    <li><a href="{{ path('app_change_password') }}" class="text-base-content hover:text-primary font-medium">Changer le mot de passe</a></li>
                                    {% if is_granted('ROLE_ADMIN') %}
                                        <li><a href="{{ path('admin') }}" class="text-base-content hover:text-primary font-medium">Tableau de bord Admin</a></li>
                                    {% endif %}
                                    <div class="divider my-1"></div>
                                    <li><a href="{{ path('app_logout') }}" class="text-base-content hover:text-primary font-medium">Déconnexion</a></li>
                                {% else %}
                                    <li><a href="{{ path('app_login') }}" class="text-base-content hover:text-primary font-medium">Se connecter</a></li>
                                    <li><a href="{{ path('app_register') }}" class="text-base-content hover:text-primary font-medium">S'inscrire</a></li>
                                {% endif %}
                            </ul>
                        </div>
                    {% else %}
                        <div class="flex items-center space-x-2">
                            <a href="{{ path('app_login') }}" class="btn btn-outline btn-primary">Se connecter</a>
                            <a href="{{ path('app_register') }}" class="btn btn-primary">S'inscrire</a>
                        </div>
                    {% endif %}
                </div>

                <!-- Hamburger Menu (Mobile) -->
                <div class="lg:hidden">
                    <div class="dropdown dropdown-end">
                        <label tabindex="0" class="btn btn-ghost">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" /></svg>
                        </label>
                        <ul tabindex="0" class="menu menu-sm dropdown-content mt-3 z-[1] p-2 shadow bg-base-100 rounded-box w-52">
                            <li><a href="/" class="text-base-content hover:text-primary text-sm font-medium">Accueil</a></li>
                            <li><a href="/product" class="text-base-content hover:text-primary text-sm font-medium">Fonctionnalités</a></li>
                            <li><a href="#" class="text-base-content hover:text-primary text-sm font-medium">Tarifs</a></li>
                            <li><a href="#" class="text-base-content hover:text-primary text-sm font-medium">Contact</a></li>
                            <div class="divider my-1"></div>
                            {% if app.user %}
                                <li><a href="{{ path('app_user_account') }}" class="text-base-content hover:text-primary text-sm font-medium">Votre Profil</a></li>
                                <li><a class="text-base-content hover:text-primary text-sm font-medium">Paramètres</a></li>
                                <li><a href="{{ path('app_change_password') }}" class="text-base-content hover:text-primary text-sm font-medium">Changer le mot de passe</a></li>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li><a href="{{ path('admin') }}" class="text-base-content hover:text-primary text-sm font-medium">Tableau de bord Admin</a></li>
                                {% endif %}
                                <div class="divider my-1"></div>
                                <li><a href="{{ path('app_logout') }}" class="text-base-content hover:text-primary text-sm font-medium">Déconnexion</a></li>
                            {% else %}
                                <li><a href="{{ path('app_login') }}" class="text-base-content hover:text-primary text-sm font-medium">Se connecter</a></li>
                                <li><a href="{{ path('app_register') }}" class="text-base-content hover:text-primary text-sm font-medium">S'inscrire</a></li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>
