{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title 'Statistiques Globales des Difficultés' %}

{% block main %}
{# <div class="mb-3"> #}
{# <a href="{{ ea_url().setController('App\\Controller\\Admin\\DifficultyCrudController').setAction('index') }}" class="btn btn-secondary"> #}
{# <i class="fa fa-arrow-left"></i> Retour à la liste #}
{# </a> #}
{# </div> #}

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Répartition des questions par difficulté</h5>
                    <div style="height: 400px;">{{ render_chart(chart) }}</div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
